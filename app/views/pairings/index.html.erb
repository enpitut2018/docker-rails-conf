<h1>Coderetreat for Girls #3 Pairing</h1>
<div id="description">
  ランダムにペアプログラミングの組を作ります。<br/>
  参加者データは運営側で管理しています。
</div>

<%= form_tag pairings_save_path, method: :get do %>
  <%= text_field_tag :name %> <%= submit_tag "Save" %>
<% end %>

<div id="parameters">
  <% if @data[:parameters] then %>
    <%= render 'layouts/parameters', parameters: @data[:parameters] %>
    <%= form_tag pairings_save_path, method: :get do %>
      <%= text_field_tag :name %> <%= submit_tag "Save" %>
    <% end %>
  <% end %>
</div>
<%= render 'layouts/pairlist',  pairs: @data[:pairs] %>

