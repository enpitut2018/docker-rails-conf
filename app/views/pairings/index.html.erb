<h1>Coderetreat for Girls #2 Pairing</h1>
<div id="description">
  ランダムにペアプログラミングの組を作ります。<br/>
  参加者数と欠番リスト（あれば。カンマ区切りで）を入力してください。
</div>

<%= form_tag '/pairings/index', method: :get do %>
  参加者数：<%= text_field_tag :number %><br/>
  欠番リスト：<%= text_field_tag :missing %>
  <%= submit_tag "Pairing" %>
<% end %>
<table>
<% @pairs.each do |pair| %>
  <tr>
  <% pair.each do |p| %>
    <td><%= p %></td>
  <% end %>
  </tr>
<% end %>
</table>

